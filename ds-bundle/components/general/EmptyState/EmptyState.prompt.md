EmptyState from task-manager-front. Use via `window.TaskManagerDS.EmptyState` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<ThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

## Examples

### Empty

```jsx
() => <EmptyState />
```
