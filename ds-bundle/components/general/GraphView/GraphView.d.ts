import * as React from 'react';

/**
 * GraphView — from task-manager-front@0.1.0.
 */
export interface GraphViewProps {
  [key: string]: unknown;
}

export declare const GraphView: React.ComponentType<GraphViewProps>;
