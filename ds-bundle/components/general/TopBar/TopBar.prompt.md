TopBar from task-manager-front. Use via `window.TaskManagerDS.TopBar` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<ThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

## Examples

### Default

```jsx
() => <TopBar />
```
